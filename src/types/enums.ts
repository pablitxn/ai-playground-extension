export enum Languages {
  en = "English",
  es = "Spanish",
  fr = "French",
  de = "German",
  it = "Italian",
  ko = "Korean",
  ru = "Russian"
}

export enum Role {
  USER = "user",
  ASSISTANT = "assistant"
}

export enum Type {
  TEXT = "text",
  ACTIVITY = "activity"
}

export enum SubType {
  MESSAGE = "message"
}
